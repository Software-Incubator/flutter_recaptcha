name: recaptchav2_plugin
description: A Flutter plugin for Google ReCAPTCHA V2. This plugin requires Webview to use Google ReCaptcha.
version: 1.0.0
author: Software-Incubator <deepanshut041@gmail.com>
homepage: https://github.com/Software-Incubator/flutter_recaptcha.git

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^0.3.4
  http: ^0.12.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    androidPackage: in.silive.recaptchav2_plugin
    pluginClass: Recaptchav2Plugin
